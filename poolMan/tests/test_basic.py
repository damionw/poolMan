from symboltables import *
from unittest import TestCase

import logging

logging.basicConfig(level=logging.INFO)

class TestProcessCreation(TestCase):
    def test_basic_functionality(self):
        self.assertTrue(False)
